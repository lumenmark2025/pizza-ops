`supabase/live-schema.json` is the checked-in snapshot of the live Supabase database schema.

Use it as the source-of-truth reference before proposing schema changes or writing migrations.

`supabase/schema.sql` is not the live database snapshot. It is only the app-side proposed schema/migration scaffold.
